---
'@finos/legend-query-builder': major
---

**BREAKING CHANGE:** The current `LambdaEditor` component has replaced by a non-inline version of lambda editor that is always in expanded mode; the previous behavior is exposed via `InlineLambdaEditor` component. Also added a flag `autoFocus` to the component to gain focus upon first rendering.

---
'@finos/legend-extension-dsl-data-space': patch
---

Fix data space quality badge tooltip copy: capitalize the first letter of the "missing requirements" sentence, and use `:` instead of `—` as the separator between a tier label and its remaining requirements.

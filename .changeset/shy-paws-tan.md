---
'@finos/legend-studio': minor
---

Add extension mechanism to allow providing documentation when hovering on grammar section parser and element keyword (e.g. `###Pure`, `###Mapping`, `Mapping`, `Class`, `Enum`, etc.) in text mode. Also allow providing suggestion snippet when creating new element in text mode.

---
'@finos/legend-extension-dsl-data-space-studio': patch
---

Update the Studio data space editor preview validation to treat `defaultRuntime` on an execution context as optional, so previewing a data space whose execution context has no default runtime no longer disables the Preview button.

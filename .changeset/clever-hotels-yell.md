---
'@finos/legend-art': major
---

**BREAKING CHANGE:** Renamed `PanelSection` to `PanelFormSection`, `PanelFormTextEditor` and `PanelFormBooleanEditor` are not renamed to `PanelFormTextField` and `PanelFormBooleanField` respectively and are now wrapped with a `PanelFormSection`.

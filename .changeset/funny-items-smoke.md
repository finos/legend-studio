---
'@finos/legend-graph': major
---

**BREAKING CHANGE:** We no longer expose `V1_ProtocolToMetaModelConnectionBuilder` and `V1_ProtocolToMetaModelEmbeddedDataBuilder`. Instead, we directly expose the builder helper `V1_buildConnection()` and `V1_buildEmbeddedData()`.

---
'@finos/legend-graph': patch
---

Add `executeLegendUserService` API to `PureGraphManager` for executing a Legend user service by URL with optional query parameters and serialization format (supports `PURE_TDSOBJECT` or default TDS execution result).
